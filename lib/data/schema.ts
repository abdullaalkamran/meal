// Shared persistence constants — used by the mock store (localStorage), the
// server database file, and the one-time legacy-localStorage → server import.

export const STORAGE_KEY = "hostel-erp:mock-db:v1";
export const SCHEMA_VERSION = 34;
